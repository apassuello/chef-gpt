# Anova AI Sous Vide Assistant - Environment Configuration Template
#
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control
#
# Setup instructions:
# 1. cp .env.example .env
# 2. Fill in your Anova account credentials
# 3. Generate a secure API key: python -c "import secrets; print('sk-anova-' + secrets.token_urlsafe(32))"
# 4. Find your device ID from Anova app or API

# ==============================================================================
# ANOVA CREDENTIALS
# ==============================================================================
# Your Anova account email and password
ANOVA_EMAIL=your-email@example.com
ANOVA_PASSWORD=your-password-here

# Your Anova Precision Cooker device ID
# Find this in the Anova app or by calling the devices API endpoint
DEVICE_ID=your-device-id-here

# ==============================================================================
# API AUTHENTICATION
# ==============================================================================
# API key for authenticating ChatGPT Custom GPT requests
# Generate with: python -c "import secrets; print('sk-anova-' + secrets.token_urlsafe(32))"
API_KEY=sk-anova-replace-with-generated-key

# ==============================================================================
# FIREBASE CONFIGURATION
# ==============================================================================
# Firebase Web API Key for Anova authentication
# Get from: https://console.firebase.google.com/ (Anova project settings)
FIREBASE_API_KEY=your-firebase-api-key

# ==============================================================================
# APPLICATION CONFIGURATION
# ==============================================================================
# Flask environment (development, production)
FLASK_ENV=development

# Enable debug mode (true/false) - NEVER use true in production
DEBUG=true

# Flask secret key for session management (generate with secrets.token_hex(32))
SECRET_KEY=replace-with-generated-secret-key

# ==============================================================================
# PRODUCTION CONFIGURATION (Raspberry Pi only)
# ==============================================================================
# Encryption key for storing credentials at rest
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# ENCRYPTION_KEY=your-encryption-key-here

# Path to encrypted credentials file (production only)
# CREDENTIALS_FILE=/opt/anova-assistant/credentials.encrypted

# ==============================================================================
# LOGGING
# ==============================================================================
# Log level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Log file path (optional, logs to stdout if not set)
# LOG_FILE=/var/log/anova-server/app.log
