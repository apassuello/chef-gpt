# Development Dependencies
# Install with: pip install -r requirements.txt -r requirements-dev.txt

# Linting and formatting
ruff>=0.4.0

# Type checking (ty from Astral - same team as ruff)
ty>=0.0.12

# Coverage reporting (also in requirements.txt but listed here for clarity)
pytest-cov>=4.0

# Optional: Pre-commit hooks
pre-commit>=3.0
